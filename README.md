# 🚀 Tower Defense Launcher

Лаунчер с автообновлением для игры Tower Defense.

## 📋 Возможности

- ✅ Автоматическая проверка обновлений при запуске
- ✅ Фоновая загрузка обновлений
- ✅ Установка обновлений одним кликом
- ✅ Красивый интерфейс лаунчера
- ✅ Поддержка Windows, Linux, macOS

## 🛠️ Установка и разработка

### Требования
- Node.js 18+
- npm или yarn

### Установка зависимостей
```bash
cd /root/tower-defense-launcher
npm install
```

### Запуск в режиме разработки
```bash
npm start
```

## 📦 Сборка

### 1. Подготовка игры

Сначала экспортируй игру из Godot:

1. Открой проект в Godot
2. `Project → Export...`
3. Экспортируй для нужной платформы в папку `tower-defense-launcher/game/`:
   - Windows: `game/TowerDefense.exe`
   - Linux: `game/TowerDefense.x86_64`
   - macOS: `game/TowerDefense.app`

### 2. Настройка GitHub

Отредактируй `package.json`, замени:
```json
"publish": {
  "provider": "github",
  "owner": "YOUR_GITHUB_USERNAME",  // ← твой username
  "repo": "tower-defense"            // ← название репозитория
}
```

### 3. Создание GitHub Token

1. Иди на https://github.com/settings/tokens
2. Generate new token (classic)
3. Выбери scope: `repo` (полный доступ)
4. Скопируй токен

### 4. Сборка установщика

```bash
# Windows
GH_TOKEN=your_token_here npm run build:win

# Linux
GH_TOKEN=your_token_here npm run build:linux

# macOS
GH_TOKEN=your_token_here npm run build:mac

# Или все платформы сразу
GH_TOKEN=your_token_here npm run build
```

Готовые файлы появятся в папке `dist/`:
- Windows: `Tower Defense Setup X.X.X.exe`
- Linux: `Tower Defense-X.X.X.AppImage`
- macOS: `Tower Defense-X.X.X.dmg`

## 🔄 Публикация обновлений

### Автоматическая публикация

```bash
# Обнови версию в package.json
npm version patch   # 1.0.0 → 1.0.1
# или
npm version minor   # 1.0.0 → 1.1.0
# или
npm version major   # 1.0.0 → 2.0.0

# Собери и опубликуй
GH_TOKEN=your_token npm run publish
```

Это автоматически:
1. Соберёт установщик
2. Создаст GitHub Release
3. Загрузит файлы

### Ручная публикация

1. Собери приложение: `npm run build`
2. Иди на GitHub → Releases → Draft new release
3. Создай тег с версией (например `v1.0.1`)
4. Загрузи файлы из `dist/`:
   - `.exe` файл
   - `.AppImage` файл  
   - `latest.yml` (важно для автообновления!)
   - `latest-linux.yml`
   - `latest-mac.yml`

## 📁 Структура проекта

```
tower-defense-launcher/
├── package.json      # Конфигурация и настройки сборки
├── main.js           # Главный процесс Electron
├── preload.js        # Preload скрипт (мост между main и renderer)
├── index.html        # UI лаунчера
├── assets/           # Иконки
│   ├── icon.png      # 512x512 PNG
│   ├── icon.ico      # Windows
│   └── icon.icns     # macOS
├── game/             # Экспортированная игра (добавляется при сборке)
│   ├── TowerDefense.exe
│   └── ...
└── dist/             # Собранные установщики
```

## 🖼️ Иконки

Для сборки нужны иконки в папке `assets/`:

1. **icon.png** - 512x512 PNG (основная)
2. **icon.ico** - Windows иконка (можно конвертировать из PNG)
3. **icon.icns** - macOS иконка

Конвертация PNG в ICO (онлайн):
- https://convertio.co/png-ico/

## ⚙️ Конфигурация автообновления

Лаунчер проверяет обновления через GitHub Releases API.

Для работы автообновления в релизе должны быть файлы:
- `latest.yml` (Windows)
- `latest-linux.yml` (Linux)
- `latest-mac.yml` (macOS)

Эти файлы генерируются автоматически при сборке.

## 🔧 Отладка

Логи автообновления: 
- Windows: `%USERPROFILE%\AppData\Roaming\tower-defense-launcher\logs\`
- Linux: `~/.config/tower-defense-launcher/logs/`
- macOS: `~/Library/Logs/tower-defense-launcher/`

## 📝 Чеклист перед релизом

- [ ] Обновить версию в `package.json`
- [ ] Экспортировать игру из Godot в `game/`
- [ ] Проверить что иконки на месте в `assets/`
- [ ] Собрать: `npm run build`
- [ ] Протестировать установщик
- [ ] Опубликовать: `npm run publish`
